version: 2

builds:
  - id: micdetector
    binary: micdetector
    env:
      - CGO_ENABLED=1
    goos:
      - darwin
    goarch:
      - arm64
      - amd64
    ldflags:
      - -s -w

archives:
  - id: micdetector
    formats: [tar.gz]
    name_template: "micdetector_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - DESIGN.md
      - config.example.json
      - com.micdetector.plist
      - justfile

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc

release:
  github:
    owner: gerco
    name: MicDetector
